import { web3 } from '@coral-xyz/anchor-31';
export function getLutSigner(programId, pubkey) {
    return web3.PublicKey.findProgramAddressSync([Buffer.from('LutSigner'), pubkey.toBuffer()], programId)[0];
}
export function getLutKey(lutSigner, lutSlot) {
    const lutKey = web3.AddressLookupTableProgram.createLookupTable({
        authority: lutSigner,
        payer: web3.PublicKey.default,
        recentSlot: BigInt(lutSlot.toString()),
    })[1];
    return lutKey;
}
//# sourceMappingURL=lookupTable.js.map