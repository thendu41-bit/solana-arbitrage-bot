"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
class ArenaClient extends __1.MarginfiClient {
    constructor(config, program, wallet, isReadOnly, group, banks, priceInfos, mintDatas, feedIdMap, addressLookupTables, preloadedBankAddresses, bankMetadataMap, bundleSimRpcEndpoint, processTransactionStrategy, lookupTablesAddresses) {
        super(config, program, wallet, isReadOnly, group, banks, priceInfos, mintDatas, feedIdMap, addressLookupTables, preloadedBankAddresses, bankMetadataMap, bundleSimRpcEndpoint, processTransactionStrategy, lookupTablesAddresses);
    }
    async processTransaction(transaction, processOpts, txOpts) {
        try {
            await fetch("/api/processTransaction", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                },
            });
        }
        catch (error) {
            console.error("Error processing transaction to the backend:", error);
        }
        return super.processTransaction(transaction, { ...processOpts, addArenaTxTag: true }, txOpts);
    }
    async processTransactions(transactions, processOpts, txOpts) {
        try {
            await fetch("/api/processTransaction", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                },
            });
        }
        catch (error) {
            console.error("Error processing transaction to the backend:", error);
        }
        return super.processTransactions(transactions, { ...processOpts, addArenaTxTag: true }, txOpts);
    }
}
exports.default = ArenaClient;
