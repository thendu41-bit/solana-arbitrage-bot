import { PublicKey } from "@solana/web3.js";
import { BankMetadataMap } from "@mrgnlabs/mrgn-common";
import { Bank } from "../../../models/bank";
import { OraclePrice } from "../../../services/price";
import { MarginfiProgram } from "../../../types";
import { HealthCacheSimulationError } from "../account.service";
import { MarginfiAccountType } from "../types";
import { EmodePair } from "../../bank";
export declare const fetchMarginfiAccountAddresses: (program: MarginfiProgram, authority: PublicKey, group: PublicKey) => Promise<PublicKey[]>;
export declare const fetchMarginfiAccountData: (program: MarginfiProgram, marginfiAccountPk: PublicKey, bankMap: Map<string, Bank>, oraclePrices: Map<string, OraclePrice>, bankMetadataMap: BankMetadataMap, emodePairs: EmodePair[]) => Promise<{
    marginfiAccount: MarginfiAccountType;
    error?: HealthCacheSimulationError;
}>;
//# sourceMappingURL=fetch.utils.d.ts.map